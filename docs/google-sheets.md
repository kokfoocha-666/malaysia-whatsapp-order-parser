# Google Sheets Workflow

The parser outputs tab-separated values.

You can copy the output directly into Google Sheets with these columns:

Name | Phone | Postcode | Address

Phone numbers are prefixed with an apostrophe so Google Sheets and Excel keep the leading zero.
